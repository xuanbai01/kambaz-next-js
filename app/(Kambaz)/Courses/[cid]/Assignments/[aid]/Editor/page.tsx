// app/(Kambaz)/Courses/[cid]/Assignments/[aid]/Editor/page.tsx
import Editor from "./editor";

export default function Page(props: any) {
  // Pass through params if/when you need them later
  return <Editor {...props} />;
}
